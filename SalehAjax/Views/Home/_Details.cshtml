@model SalehAjax.Models.Person
<td>
    @{
        ViewBag.Title = "Details";
    }
    <h2>Details</h2>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.City)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.City)
            </dd>

        </dl>
    </div>
    <!--if i need to change from ActionLink to BeginForm to use button istead of Link then I have to change as it is down -->
    
    @*<div class="form-group">
    @Ajax.ActionLink("Edit", "_Edit", new { id = Model.Id }, new AjaxOptions
    {
        UpdateTargetId = "PersonId" + Model.Id,//it will replace the same <p> up id="newCreate"
        InsertionMode = InsertionMode.Replace,
        HttpMethod = "GET"
    })
        <a>
            |@Ajax.ActionLink("Cancel", "PrtialIndex", new { id = Model.Id }, new AjaxOptions
            {
                UpdateTargetId = "PersonId" + Model.Id,//it will replace the same <p> up id="newCreate"
                InsertionMode = InsertionMode.ReplaceWith,//replace with because we need to replace all info
                HttpMethod = "GET"
            })
        </a>
    </div>*@
    

    @using (Ajax.BeginForm("_Edit", "Home", new { id = Model.Id }, new AjaxOptions
    {
        UpdateTargetId = "PersonId" + Model.Id,//it will replace the same <p> up id="newCreate"
        InsertionMode = InsertionMode.Replace,
        HttpMethod = "GET"
    }))
    {
        @Html.AntiForgeryToken()
        <div class="form-actions no-color">
            <!--confirm delete we resend the id as input by ConfirmDelete method-->
            <input type="submit" value="Edit" class="btn btn-success" />|
            <!--Back To List-->
            <a>

                @Ajax.ActionLink("Cancel", "PrtialIndex", new { id = Model.Id }, new AjaxOptions
                {
                    UpdateTargetId = "PersonId" + Model.Id,//it will replace the same <p> up id="newCreate"
                    InsertionMode = InsertionMode.ReplaceWith,//replace with because we need to replace all info
                    HttpMethod = "GET"
                })
            </a>
        </div>
    }
</td>
